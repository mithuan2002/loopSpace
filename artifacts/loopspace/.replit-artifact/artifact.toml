kind = "web"
previewPath = "/"
title = "LoopSpace"
version = "1.0.0"
id = "artifacts/loopspace"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/loopspace run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/loopspace", "run", "build" ]
serve = "static"
publicDir = "artifacts/loopspace/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
