{
  "framework": null,
  "installCommand": "corepack enable && pnpm install",
  "buildCommand": "pnpm -w run typecheck:libs && pnpm --filter @workspace/api-server run build && BASE_PATH=/ pnpm --filter @workspace/loopspace run build && cp -r artifacts/loopspace/dist/public/. public",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
